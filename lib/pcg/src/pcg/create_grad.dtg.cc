// THIS FILE WAS AUTO-GENERATED BY proj. DO NOT MODIFY IT!
// If you would like to modify this datatype, instead modify
// lib/pcg/include/pcg/create_grad.enum.toml
/* proj-data
{
  "generated_from": "9fd617027e850b6d6db476a49b3e0334"
}
*/

#include "pcg/create_grad.dtg.h"

#include <sstream>
#include <stdexcept>

namespace std {
size_t hash<FlexFlow::CreateGrad>::operator()(FlexFlow::CreateGrad x) const {
  return std::hash<int>{}(static_cast<int>(x));
}
} // namespace std
namespace FlexFlow {
std::string format_as(CreateGrad x) {
  switch (x) {
    case CreateGrad::YES:
      return "YES";
    case CreateGrad::NO:
      return "NO";
    default:
      std::ostringstream oss;
      oss << "Unknown CreateGrad value " << static_cast<int>(x);
      throw std::runtime_error(oss.str());
  }
}
std::ostream &operator<<(std::ostream &s, CreateGrad x) {
  return s << fmt::to_string(x);
}
} // namespace FlexFlow
namespace FlexFlow {
void to_json(::nlohmann::json &j, CreateGrad x) {
  switch (x) {
    case CreateGrad::YES:
      j = "YES";
      break;
    case CreateGrad::NO:
      j = "NO";
      break;
    default:
      std::ostringstream oss;
      oss << "Unknown CreateGrad value " << static_cast<int>(x);
      throw std::runtime_error(oss.str());
  }
}
void from_json(::nlohmann::json const &j, CreateGrad &x) {
  std::string as_str = j.get<std::string>();
  if (as_str == "YES") {
    x = CreateGrad::YES;
  } else if (as_str == "NO") {
    x = CreateGrad::NO;
  } else {
    std::ostringstream oss;
    oss << "Unknown CreateGrad value " << as_str;
    throw std::runtime_error(oss.str());
  }
}
} // namespace FlexFlow
namespace rc {
Gen<FlexFlow::CreateGrad> Arbitrary<FlexFlow::CreateGrad>::arbitrary() {
  return gen::element<FlexFlow::CreateGrad>(FlexFlow::CreateGrad::YES,
                                            FlexFlow::CreateGrad::NO);
}
} // namespace rc
