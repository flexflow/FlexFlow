#include "parallel_computation_graph.h"

namespace FlexFlow {

ParallelTensor ParallelComputationGraph::create_parallel_tensor(ParallelTensorShape const &shape,
                                                                CreateGrad create_grad, 
                                                                optional<tensor_guid_t> tensor_guid)
{
  
}

}
