// THIS FILE WAS AUTO-GENERATED BY proj. DO NOT MODIFY IT!
// If you would like to modify this datatype, instead modify
// lib/utils/include/utils/graph/serial_parallel/parallel.struct.toml
/* proj-data
{
  "generated_from": "0aee46f91f8e9ae0f18e1f496aa886b4"
}
*/

#include "utils/graph/serial_parallel/parallel.dtg.h"

#include "utils/fmt/variant.h"
#include "utils/fmt/vector.h"
#include "utils/hash/vector.h"
#include <sstream>

namespace FlexFlow {
Parallel::Parallel(
    std::vector<std::variant<::FlexFlow::Serial, ::FlexFlow::Node>> const
        &children)
    : children(children) {}
bool Parallel::operator==(Parallel const &other) const {
  return std::tie(this->children) == std::tie(other.children);
}
bool Parallel::operator!=(Parallel const &other) const {
  return std::tie(this->children) != std::tie(other.children);
}
bool Parallel::operator<(Parallel const &other) const {
  return std::tie(this->children) < std::tie(other.children);
}
bool Parallel::operator>(Parallel const &other) const {
  return std::tie(this->children) > std::tie(other.children);
}
bool Parallel::operator<=(Parallel const &other) const {
  return std::tie(this->children) <= std::tie(other.children);
}
bool Parallel::operator>=(Parallel const &other) const {
  return std::tie(this->children) >= std::tie(other.children);
}
} // namespace FlexFlow

namespace std {
size_t
    hash<FlexFlow::Parallel>::operator()(::FlexFlow::Parallel const &x) const {
  size_t result = 0;
  result ^=
      std::hash<
          std::vector<std::variant<::FlexFlow::Serial, ::FlexFlow::Node>>>{}(
          x.children) +
      0x9e3779b9 + (result << 6) + (result >> 2);
  return result;
}
} // namespace std

namespace FlexFlow {
std::string format_as(Parallel const &x) {
  std::ostringstream oss;
  oss << "<Parallel";
  oss << " children=" << x.children;
  oss << ">";
  return oss.str();
}
std::ostream &operator<<(std::ostream &s, Parallel const &x) {
  return s << fmt::to_string(x);
}
} // namespace FlexFlow
