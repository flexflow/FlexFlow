#include "utils/testing.h"
#include "utils/type_index_extra/matches.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("matches(std::type_index)") {
    CHECK_MESSAGE(false, "TODO: matches(std::type_index)");
  }
}
