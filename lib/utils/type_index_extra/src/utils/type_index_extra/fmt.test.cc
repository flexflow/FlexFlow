#include "utils/type_index_extra/fmt.h"
#include "utils/testing.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("fmt::formatter<::std::type_index>") {
    CHECK_MESSAGE(false, "TODO: fmt::formatter<::std::type_index>");
  }
}
