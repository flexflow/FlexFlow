#include "utils/testing.h"
#include "utils/hash_extra/instances/tuple.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("hash<std::tuple<...>>") {
    CHECK_MESSAGE(false, "TODO: hash<std::tuple<...>>");
  }
}
