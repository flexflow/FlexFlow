#include "utils/testing.h"
#include "utils/hash_extra/instances/unordered_set.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("std::hash<std::unordered_set<T>>") {
    CHECK_MESSAGE(false, "TODO: std::hash<std::unordered_set<T>>");
  }
}
