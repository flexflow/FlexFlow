#include "utils/testing.h"
#include "utils/hash_extra/instances/vector.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("std::hash<std::vector<T>>") {
    CHECK_MESSAGE(false, "TODO: std::hash<std::vector<T>>");
  }
}
