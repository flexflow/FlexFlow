#include "utils/testing.h"
#include "utils/hash_extra/instances/pair.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("hash<std::pair<L, R>>") {
    CHECK_MESSAGE(false, "TODO: hash<std::pair<L, R>>");
  }
}
