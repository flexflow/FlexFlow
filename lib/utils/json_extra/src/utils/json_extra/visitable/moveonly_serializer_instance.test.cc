#include "utils/json_extra/visitable/moveonly_serializer_instance.h"
#include "utils/testing.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("adl_serializer for moveonly visitable") {
    CHECK_MESSAGE(false, "TODO: adl_serializer for moveonly visitable");
  }
}
