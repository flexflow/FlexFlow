#include "utils/testing.h"
#include "utils/json_extra/visitable/required.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("adl_serializer<::FlexFlow::req<T>>") {
    CHECK_MESSAGE(false, "TODO: adl_serializer<::FlexFlow::req<T>>");
  }
}
