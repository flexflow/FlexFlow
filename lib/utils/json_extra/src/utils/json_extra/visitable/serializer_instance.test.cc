#include "utils/json_extra/visitable/serializer_instance.h"
#include "utils/testing.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("adl_serializer for default constructible visitable") {
    CHECK_MESSAGE(false,
                  "TODO: adl_serializer for default constructible visitable");
  }
}
