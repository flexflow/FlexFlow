#include "utils/testing.h"
#include "utils/json_extra/variant/serializer_instance.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("adl_serializer<std::variant<Args...>>") {
    CHECK_MESSAGE(false, "TODO: adl_serializer<std::variant<Args...>>");
  }
}
