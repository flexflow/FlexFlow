ff_add_library(
  NAME
    utils
  SRC_PATTERNS
    src/*.cc
  PUBLIC_INCLUDE
    include/
  PRIVATE_INCLUDE
    src/
  DEPS
    expected
    visit_struct
    fmt
    json
    cuda
    doctest
)      

add_subdirectory(ffi)
add_subdirectory(test)
