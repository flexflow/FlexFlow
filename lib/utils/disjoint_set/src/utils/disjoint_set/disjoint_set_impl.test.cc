#include "utils/testing.h"
#include "utils/disjoint_set/disjoint_set_impl.h"

TEST_SUITE(FF_TEST_SUITE) {
  TEST_CASE("disjoint_set_impl") {
    CHECK_MESSAGE(false, "TODO: disjoint_set_impl");
  }
}
