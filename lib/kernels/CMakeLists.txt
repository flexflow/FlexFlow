ff_add_library(
  NAME
    kernels
  SRC_PATTERNS
    src/*.cc
  PUBLIC_INCLUDE
    include/
  PRIVATE_INCLUDE
    src/
  DEPS
    op-attrs
    cuda
    cudnn
    nccl
)      
set_target_properties(
  ${project_target} 
  PROPERTIES 
  CUDA_STANDARD 17
)
